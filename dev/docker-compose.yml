services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: ha-techem-dev
    ports:
      - "8123:8123"
    restart: unless-stopped
    volumes:
      - ./config:/config
      - ../custom_components/techem:/config/custom_components/techem
    environment:
      - TZ=Europe/Oslo
